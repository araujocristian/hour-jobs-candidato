import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: 50,
    paddingTop: 20,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});
