import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    maxHeight: 62,
    paddingTop: 20,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});
